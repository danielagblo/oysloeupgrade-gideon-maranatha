#!/usr/bin/expect -f

set timeout 30

spawn ssh -o StrictHostKeyChecking=no root@207.154.219.165
expect "password:"
send "qazRyb-kexfy6-ketken\r"
expect "# "

send "echo '=== MOVING .ENV.PRODUCTION FILE BACK TO BACKEND DIRECTORY ==='\r"
expect "# "
send "mv ~/.env.production ~/oysloeupgrade-gideon-maranatha-main/backend/\r"
expect "# "

send "echo '=== VERIFYING FILE WAS MOVED BACK TO BACKEND DIRECTORY ==='\r"
expect "# "
send "ls -la ~/oysloeupgrade-gideon-maranatha-main/backend/.env.production\r"
expect "# "
send "ls -la ~/.env.production 2>/dev/null || echo 'File successfully moved back to backend directory'\r"
expect "# "

send "echo '.env.production file successfully moved back to backend directory'\r"
expect "# "

send "exit\r"
expect eof
